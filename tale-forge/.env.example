# Tale Forge - Frontend-Only Example Environment Variables
# This is a frontend-only app - all secrets are in Supabase Edge Function environment

# --- Required: Supabase Connection ---
VITE_SUPABASE_URL=https://your-project-id.supabase.co
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key

# --- Required: App Configuration ---
VITE_BASE_URL=http://localhost:3001
VITE_APP_ENV=development

# --- Environment ---
NODE_ENV=development

# IMPORTANT: This is a frontend-only app!
# - Copy this to .env and fill in your actual values
# - All API secrets (OpenAI, OVH, etc.) are stored in Supabase Edge Function secrets
# - Only public/anon keys go in frontend environment variables
# - For Vercel deployment, set these in Vercel dashboard environment variables